# NetPulse - Python 依赖
# 核心依赖 (必需)
# tkinter 是 Python 内置模块，无需单独安装

# 可选依赖 (增强功能)
# scapy: DHCP 多服务器检测 (需要先安装 Npcap)
# 安装 Npcap: https://npcap.com/dist/npcap-1.79.exe (勾选 "WinPcap API-compatible Mode")
scapy>=2.5.0
# cryptography: 修 scapy.modules.krack 的 ImportError (scapy 可选依赖)
cryptography>=3.4

# speedtest: Speedtest.net 测速
speedtest-cli>=2.1.3

# reportlab: PDF 报告导出 (可选, 缺失时自动降级导出 HTML)
reportlab>=4.0.0

# PyInstaller: 打包为单文件 exe
pyinstaller>=6.0.0
